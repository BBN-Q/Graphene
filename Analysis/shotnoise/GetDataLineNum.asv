function [ output_arg ] = GetDataLineNum( FileName )
%UNTITLED Summary of this function goes here
%   Detailed explanation goes here
fid = fopen(FileName);
iHeadLineNum = 0;
str = fgetl(fid);
while (str ~= -1)
    iHeadLineNum = iHeadLineNum+1;
    str = fgetl(fid)
    if strcmp(str, '### Note ###')==1
        str = -1;
    end
    if strcmp(str, '')==1
end
fclose(fid);
output_arg = iHeadLineNum
end

